<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="text_detail">
        <item name="android:textColor">@color/text_detail</item>
        <item name="android:text">@string/ca_ac</item>
        <item name="android:textSize">@dimen/text_detail</item>
    </style>

    <style name="header">
        <item name="android:textColor">@color/header</item>
        <item name="android:textSize">@dimen/header</item>
    </style>

    <style name="detail">
        <item name="android:textColor">@color/detail</item>
        <item name="android:textSize">@dimen/detail</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
